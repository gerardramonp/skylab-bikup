import dispatch from '../../dispatcher';
import bikeDetailStore from '../../stores/bikeDetailStore';
import actionTypes from '../../actions/bikeDetailActions';

describe('Bike Detail Store', () => {
	it('should work', () => {
		expect(true).toBe(true);
	});
});
